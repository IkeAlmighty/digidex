{
  "name": "express-calendar-app",
  "version": "1.0.0",
  "description": "A mobile first calendar web app for more granular event sharing",
  "main": "server.js",
  "scripts": {
    "test": "cd server && npm run test",
    "dev": "concurrently \"cd client & npm run dev\" \"cd server && npm run dev\"",
    "seed": "cd server && npm run seed",
    "start": "cd server && npm run start",
    "build": "cd client && npm run build",
    "install": "cd client && npm i && cd ../server && npm i"
  },
  "author": "ikealmighty",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "helmet": "^8.1.0"
  }
}
